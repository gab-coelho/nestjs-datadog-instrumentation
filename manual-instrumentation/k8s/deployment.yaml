apiVersion: v1
kind: Namespace
metadata:
  name: nestjs-manual
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nestjs-manual
  namespace: nestjs-manual
  labels:
    app.kubernetes.io/name: nestjs-manual
    app.kubernetes.io/part-of: datadog-instrumentation-lab
    tags.datadoghq.com/env: dev
    tags.datadoghq.com/service: nestjs-manual
    tags.datadoghq.com/version: local
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nestjs-manual
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nestjs-manual
        app.kubernetes.io/part-of: datadog-instrumentation-lab
        tags.datadoghq.com/env: dev
        tags.datadoghq.com/service: nestjs-manual
        tags.datadoghq.com/version: local
    spec:
      containers:
        - name: app
          image: localhost:5001/nestjs-dd-manual:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: nestjs-manual-config
          env:
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: DD_ENV
              value: dev
            - name: DD_SERVICE
              value: nestjs-manual
            - name: DD_VERSION
              value: local
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
